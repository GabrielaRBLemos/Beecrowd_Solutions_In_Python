n = int(input())
inside_range = 0
outside_range = 0
for i in range(n):
    x = int(input())
    if x >= 10 and x <= 20:
        inside_range = inside_range + 1
    else:
        outside_range = outside_range + 1
print(f"{inside_range} in")
print(f"{outside_range} out")
